# Session Log — March 30, 2026 (Monday)
## Week 3, Day 1 — Go Backend Removal + Paynow Integration

### What I Did
- **Decision: Supabase over Go** — removed entire Go backend (-9,548 lines, 43 files)
- Removed Go frontend layer (goApi, goWebSocket, goAuthStore, hooks/go/)
- Rotated leaked Supabase access token, cleaned hardcoded keys from settings
- Deleted 3 dead branches (backup/main-before-merge, benchmark/paystack, benchmark/stripe)
- Confirmed table name consistency (livestock_items everywhere)
- Added stock card / vet certificate upload to PostListing
- Fixed critical payment bug: EcoCash/OneMoney selections were silently ignored (always saved as 'Card')
- Fixed missing livestockTitle in checkout → Stripe now shows item name

#### Paynow Integration (Major)
- Rewrote `initiate-payment` Edge Function: Paynow primary, Stripe card fallback
- Added EcoCash/OneMoney express checkout via `/interface/remotetransaction` (USSD prompt to phone)
- Added web checkout via `/interface/initiatetransaction` (redirect to Paynow page)
- Rewrote `payment-webhook` to handle both Paynow (URL-encoded + SHA-512 hash) and Stripe (JSON + signature)
- Browser-relay fallback: if Edge Function can't reach Paynow (Cloudflare), browser POSTs directly
- **First successful Paynow transaction initiated** — got `status=Ok` with valid `browserurl`

#### Security
- Moved all secrets to `.env.local` (gitignored) and Supabase Edge Function secrets
- Set Paynow keys via `supabase secrets set`
- Added Supabase access token to `~/.zshrc`
- Cleaned `.claude/settings.json` of all hardcoded tokens

### Blockers
- EcoCash not showing on Paynow payment page — merchant config issue
- pg_cron not enabled — auctions don't auto-end

### Commits (6)
- Week 2 cleanup: remove Go backend, fix payments, add stock card upload
- Switch primary payment gateway to Paynow with Stripe card fallback
- Secure secrets: use env vars and Supabase secrets, not hardcoded keys
- Fix Paynow payment flow: call API directly, redirect to browserurl
- Fix Paynow fallback: browser fetches API, parses response, redirects
- Add EcoCash/OneMoney express checkout via remotetransaction endpoint

### Hours: ~8h
